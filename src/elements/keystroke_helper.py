from typing import List
"""
    Used for getting the status of a keyboard (which key is pressed), and generates a timestamp.
"""
class KeystrokeHelper:

    @staticmethod
    def get_keys_pressed() -> List[str]:
        pass
    #    return list(keyboard.get_typed_strings())

        